services:
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: plaid-api
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - ./apps/api/.env

